# Project Name

## Description
A sky star map in 3D with solar planets and constellations.

## Installation
1. Clone the repository.
2. Navigate to the project directory.
3. Run `npm install` to install the dependencies.

## Usage
To start the project, open a terminal and run the following command:
npm start

